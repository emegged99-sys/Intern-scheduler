# פרסום לאוויר (חינם)

הגרסה החדשה מגישה הכול משרת אחד — מסך המנהל וגם מסך המתמחה. **Netlify כבר לא
נחוץ**; אפשר להשאיר אותו או למחוק, ההחלטה שלכם (סעיף 7).

זמן משוער: 20 דקות. עלות: 0 ₪.

---

## שלב 0 — גיבוי לפני הכול ⚠

**זה השלב שאסור לדלג עליו.** הנתונים שלכם שמורים כרגע כקבצים על שרת Render,
ומערכת הקבצים שם נמחקת בכל דיפלוי. ברגע שתעלו את הגרסה החדשה — הם ייעלמו.

1. פתחו את העורך הקיים שלכם (זה שב-Netlify), כפי שאתם עושים תמיד.
2. המתינו שייטען את הנתונים מהשרת (הסטטוס למעלה).
3. לחצו **💾 גיבוי** → נשמר קובץ `scheduler_backup_<תאריך>.json`.
4. שמרו אותו במקום בטוח. הוא מכיל מתמחים, תורני חוץ וחגים.

הגרסה החדשה מנסה לייבא לבד קבצים ישנים אם הם עדיין קיימים, אבל אל תסמכו על
זה — הגיבוי הידני הוא הביטוח.

---

## שלב 1 — בסיס נתונים חינמי (Neon)

בלי זה הנתונים ימשיכו להימחק בכל דיפלוי.

1. [neon.tech](https://neon.tech) → הרשמה (אפשר עם GitHub) → **New project**.
2. אזור: **AWS eu-central-1 (Frankfurt)** — הקרוב לישראל.
3. אחרי היצירה מוצגת **Connection string**. העתיקו אותה. היא נראית כך:
   ```
   postgresql://user:pass@ep-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require
   ```

התוכנית החינמית: 0.5GB, בלי תפוגה, בלי כרטיס אשראי. חודש שלם אצלכם שוקל
פחות מ-1MB.

**בדיקה מקומית (מומלץ, דקה):**
```powershell
python -m pip install "psycopg[binary]"
$env:DATABASE_URL='postgresql://…'
python app.py
```
בשורת «בסיס נתונים» אמור להופיע `Postgres` ולא `SQLite`. אם כן — המחרוזת
תקינה. עצרו (Ctrl+C) ונקו: `Remove-Item Env:DATABASE_URL`.

---

## שלב 2 — העלאת הקוד ל-GitHub

בתיקייה של הפרויקט:

```powershell
git add -A
git commit -m "portal, freeze, month archive"
git push -u origin feature/portal-lock-archive
```

הענף `main` לא נגע — אם משהו ישתבש, אפשר לחזור אליו בלחיצה.

---

## שלב 3 — הגדרות ב-Render

בשירות הקיים → **Environment** → הוסיפו שלושה משתנים
(`APP_PASSWORD` כבר קיים אצלכם):

| Key | Value | למה |
|---|---|---|
| `DATABASE_URL` | המחרוזת מ-Neon | בלעדיה הנתונים נמחקים בכל דיפלוי |
| `SECRET_KEY` | מחרוזת אקראית ארוכה | מצפינה את הסשן של המתמחים; בלעדיה כל המתמחים מנותקים בכל ריסטארט |
| `SECURE_COOKIES` | `1` | קוקי רק מעל HTTPS |

ליצירת `SECRET_KEY`:
```powershell
python -c "import secrets; print(secrets.token_hex(32))"
```

---

## שלב 4 — דיפלוי

**Settings** → ודאו:
- **Branch:** `feature/portal-lock-archive`
- **Build Command:** `pip install -r requirements.txt`
- **Start Command:** ריק (יילקח מ-`Procfile`) או:
  `gunicorn app:app --timeout 900 --workers 1 --threads 4 --bind 0.0.0.0:$PORT`

ואז **Manual Deploy** → **Deploy latest commit**. הבנייה לוקחת 2–4 דקות.

---

## שלב 5 — בדיקה

לפי הסדר:

| # | כתובת | מה אמור לקרות |
|---|---|---|
| 1 | `https://<השירות>.onrender.com/health` | `{"status":"ok"}` |
| 2 | `https://<השירות>.onrender.com` | עמוד בית; בשורת «בסיס נתונים» כתוב **Postgres** |
| 3 | `.../editor` | מסך המנהל; מבקש סיסמת שרת פעם אחת |
| 4 | `.../portal/` | מסך כניסה עם קוד |

אם בשלב 2 כתוב SQLite — `DATABASE_URL` לא נקלט. חזרו לשלב 3.

---

## שלב 6 — שחזור הנתונים

במסך המנהל (`/editor`):

1. **📂 שחזור** → בחרו את קובץ הגיבוי משלב 0.
2. ודאו שהמתמחים חזרו, ושהם נשמרו לשרת (סטטוס הסנכרון למעלה).
3. לשונית **חודשים** → ודאו שיש חודש נוכחי.
4. לשונית **חודשים** → **קודי גישה** → הקודים נוצרו לכל המתמחים.

---

## שלב 7 — Netlify

השרת מגיש עכשיו גם את מסך המנהל, אז אפשר פשוט להפסיק להשתמש ב-Netlify
ולעבוד מול `/editor`. יתרון: תמיד מסונכרן עם השרת, אין גרסה ישנה שנתקעת.

אם אתם רוצים להמשיך איתו בכל זאת — גררו את `intern_editor.html` **החדש** ל-
[app.netlify.com/drop](https://app.netlify.com/drop), אחרת תישאר שם הגרסה
הישנה בלי לשונית «חודשים».

---

## שלב 8 — חלוקה למתמחים

לשונית **חודשים** → **קודי גישה**. לכל אחד קוד קבוע בן 6 תווים.
שלחו לכל מתמחה את הקוד שלו ואת הכתובת:

```
https://<השירות>.onrender.com/portal/
```

הודעה מוכנה לשליחה:

> היי, מעכשיו מזינים תורנויות כאן: <קישור>
> הקוד האישי שלך: XXXXXX
> אפשר לסמן ימים שאי אפשר לתרון בהם וימים שמעדיפים. אחרי שהשיבוץ ייסגר
> תוכל/י לראות שם את הלוח המלא ואת התורנויות שלך.

---

## מה מוותרים עליו בחינם

- **השירות נרדם** אחרי 15 דקות בלי תעבורה; ההתעוררות ~דקה. הכניסה הראשונה
  של הבוקר איטית.
- **הרצת שיבוץ: 3–6 דקות** (מעבד חלש). **השאירו את החלון פתוח** עד הסוף —
  סגירה באמצע עלולה להרוג את ההרצה. חודש שכבר נשמר לא נפגע.
- **אין גיבוי אוטומטי ב-Neon החינמית** — אחרי כל הקפאת חודש הורידו אקסל
  מלשונית «חודשים», ומדי פעם **💾 גיבוי**.

אם ההירדמות מציקה: Render Starter, 7$ לחודש, מבטל אותה ומאיץ את ההרצה.

---

## פתרון תקלות

| מה קורה | הסיבה |
|---|---|
| הבנייה נכשלת על `psycopg` | גרסת פייתון לא נתמכת. יש בריפו `.python-version` עם 3.12.7 — ודאו שהועלה |
| עמוד הבית מראה SQLite | `DATABASE_URL` לא הוגדר או שגוי |
| מתמחים מנותקים כל הזמן | `SECRET_KEY` לא הוגדר |
| 502 בזמן יצירת שיבוץ | ההרצה עברה את הזמן. ודאו `--timeout 900` ב-Start Command |
| הנתונים ריקים אחרי דיפלוי | שחזרו מהגיבוי (שלב 6) |
| לשונית «חודשים» חסרה | פתחתם את Netlify הישן. עברו ל-`/editor` |
