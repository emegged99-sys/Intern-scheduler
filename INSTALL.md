# התקנה והפעלה

שני מסלולים. **התחילו במסלול א׳** — הריצו על המחשב שלכם, בדקו שהכול עובד, ורק
אז עדכנו את הגרסה החיה.

---

## מסלול א׳ — הרצה על המחשב (Windows)

### 1. חילוץ הקבצים

הורידו את ה-ZIP, לחצו עליו קליק ימני → **Properties** → סמנו **Unblock** למטה →
**Apply**, ואז **Extract All** לתיקייה נוחה, למשל `C:\Users\<שם>\scheduler`.

(ה-Unblock נחוץ כי Windows חוסם קבצים שהורדו מהאינטרנט.)

### 2. פתיחת PowerShell בתיקייה

פתחו PowerShell ועברו לתיקייה שחילצתם:

```powershell
cd C:\Users\<שם>\scheduler
```

בדקו שאתם במקום הנכון — הפקודה הבאה צריכה להציג את הקובץ ולא שגיאה:

```powershell
dir app.py
```

### 3. התקנת החבילות

```powershell
python -m pip install flask flask-cors openpyxl
```

**לא** להריץ `pip install -r requirements.txt` על Windows: הקובץ הזה מיועד
לשרת ומכיל שתי חבילות שיפילו את ההתקנה כאן —
`gunicorn` (שרת ל-Linux בלבד) ו-`psycopg` (נחוץ רק כשעובדים מול Postgres בענן,
ולגרסאות פייתון חדשות מאוד לא תמיד יש לו wheel מוכן). אם ההתקנה נופלת על
אחת מהן, שום דבר לא מותקן — כולל flask.

שלוש החבילות למעלה זה כל מה שצריך להרצה מקומית.

אם `python` לא מוכר, התקינו מ-[python.org](https://python.org) ובמסך ההתקנה
**סמנו "Add Python to PATH"**.

### 4. הפעלה

```powershell
$env:APP_PASSWORD='בחרו-סיסמה'
python app.py
```

שימו לב: ב-PowerShell זה `$env:` — לא `export` ולא `set`.
המשתנה חי רק בחלון הזה, אז אם תסגרו ותפתחו טרמינל חדש, הגדירו שוב.

אמורות להופיע השורות:

```
  שרת השיבוץ פועל
  מסך מתמחה   : http://localhost:5000/portal/
  כתובת לעורך : http://localhost:5000
  בסיס נתונים : SQLite · ...\data\scheduler.db
```

**השאירו את החלון פתוח** — סגירתו עוצרת את השרת.

### 5. פתיחת מסך המנהל

בדפדפן: **http://localhost:5000** — עמוד הבית מציג קישור למסך המנהל ולמסך
המתמחה. או ישירות:

| מסך | כתובת |
|---|---|
| מנהל | `http://localhost:5000/editor` |
| מתמחה | `http://localhost:5000/portal/` |

כשהעורך מוגש מהשרת, כתובת השרת מזוהה לבד — צריך רק להזין **סיסמת שרת**
(מסעיף 4) בלשונית «יצירת שיבוץ». הסיסמה נשמרת בדפדפן.

מהמחשב שלכם `localhost` עובד; ממכשיר אחר ברשת החליפו ב-IP שמוצג בחלון השרת
(למשל `http://10.100.102.28:5000/portal/`) — נוח לבדיקה מהטלפון.

### 6. הרצה ראשונה מקצה לקצה

1. לשונית **חודשים** → רואים שנוצר חודש נוכחי. אפשר **+ חודש חדש** לחודש אחר.
2. לשונית **מתמחים** → ייבוא CSV או עריכה ידנית.
3. **תורני חוץ** ו**חגים** לפי הצורך.
4. לשונית **חודשים** → **קודי גישה** → מעתיקים את הקוד של מתמחה כלשהו.
5. פותחים `http://localhost:5000/portal/` ומתחברים עם הקוד — כך המתמחה רואה
   את המסך שלו. אפשר לסמן ימים ולשמור.
6. חזרה לעורך → **יצירת שיבוץ** → **צור שיבוץ**. לוקח כדקה. השיבוץ נשמר
   לארכיון אוטומטית.
7. לשונית **חודשים** → **🔒 הקפאת השיבוץ**.
8. רענון של מסך המתמחה — עכשיו הוא רואה את הלוח המלא ואת התורנויות שלו.

זהו. אם כל זה עבד, המסלול השני הוא רק העתקה של אותו דבר לענן.

---

## מסלול ב׳ — עדכון הגרסה החיה

הגרסה שלכם באוויר מורכבת משני חלקים, ושניהם צריכים עדכון:
ה-**backend** ב-Render וה-**עורך** ב-Netlify.

### 1. העלאת הקוד ל-GitHub

בתיקייה שחילצתם יש כבר repo עם הענף החדש והריפו שלכם מוגדר כ-remote:

```powershell
git push -u origin feature/portal-lock-archive
```

הענף `main` לא נגע. אפשר לבדוק את הענף בנפרד לפני מיזוג.

### 2. בסיס נתונים חינמי (חובה — אחרת הנתונים נמחקים)

הנתונים שלכם נשמרים כרגע כקבצים על השרת. **בתוכנית החינמית של Render מערכת
הקבצים נמחקת בכל דיפלוי ובכל התעוררות משינה**, ולכן צריך להוציא אותם החוצה:

1. הרשמה ב-[neon.tech](https://neon.tech) → **New project** → אזור
   **AWS eu-central-1 (Frankfurt)**.
2. להעתיק את ה-**Connection string** (נראה כמו
   `postgresql://user:pass@ep-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require`).

### 3. הגדרות ב-Render

בשירות הקיים → **Environment** → להוסיף שלושה משתנים
(`APP_PASSWORD` כבר קיים):

| Key | Value |
|---|---|
| `DATABASE_URL` | המחרוזת מ-Neon |
| `SECRET_KEY` | מחרוזת אקראית ארוכה — מצפינה את הסשן של המתמחים |
| `SECURE_COOKIES` | `1` |

ואז **Manual Deploy** → בחירת הענף `feature/portal-lock-archive`.

בהפעלה הראשונה, אם נשארו קבצי `data/*.json` הם מיובאים אוטומטית לחודש הנוכחי,
כך שהנתונים הקיימים לא הולכים לאיבוד.

בדיקה: `https://<השירות>.onrender.com/health` → `{"status":"ok"}`.

### 4. עדכון העורך ב-Netlify

גררו את ה-`intern_editor.html` **החדש** ל-
[app.netlify.com/drop](https://app.netlify.com/drop) (או לאתר הקיים → Deploys →
גרירה). בלי זה תמשיכו לראות את הגרסה הישנה, בלי לשונית **חודשים**.

### 5. חלוקת הקודים למתמחים

לשונית **חודשים** → **קודי גישה**. לכל מתמחה יש קוד קבוע בן 6 תווים.
שלחו לכל אחד את הקוד שלו ואת הכתובת:

```
https://<השירות>.onrender.com/portal/
```

---

## דברים שכדאי לדעת

**ההרצה בענן איטית יותר** — 3 עד 6 דקות במקום דקה, כי המעבד החינמי חלש.
השאירו את החלון פתוח עד שהיא נגמרת; אם תסגרו באמצע היא עלולה למות. חודש
שכבר נשמר לא נפגע.

**השירות נרדם** אחרי 15 דקות בלי שימוש, וההתעוררות לוקחת כדקה. הכניסה הראשונה
של הבוקר תהיה איטית.

**גיבוי** — ל-Neon החינמית אין גיבוי ארוך טווח. אחרי כל הקפאת חודש, הורידו
את האקסל מלשונית **חודשים** ושמרו אצלכם.

---

## פתרון תקלות

| מה קורה | הסיבה |
|---|---|
| `export : The term 'export' is not recognized` | זו תחביר של Linux. ב-PowerShell: `$env:APP_PASSWORD='...'` |
| `python : command not found` | פייתון לא מותקן או לא ב-PATH. התקנה מחדש עם "Add Python to PATH" |
| `ModuleNotFoundError: No module named 'flask'` | סעיף 3 לא רץ או נפל. הריצו `python -m pip install flask flask-cors openpyxl` |
| ההתקנה נופלת על `gunicorn` או `psycopg` | הרצתם `-r requirements.txt`. על Windows מתקינים רק את שלוש החבילות שבסעיף 3 |
| `dir app.py` מחזיר שגיאה | אתם בתיקייה הלא נכונה — חפשו את התיקייה שבה יושבים `app.py` ו-`monthly_scheduler.py` |
| הדפדפן מראה 401 | הסיסמה בעורך לא תואמת ל-`APP_PASSWORD`, או נשמרה עם רווח |
| לשונית **חודשים** לא מופיעה | פתחתם את הקובץ הישן. חלצו מחדש, או עדכנו את Netlify |
| `Not Found` על כתובת השרת | גרסה ישנה של `app.py`. בחדשה יש עמוד בית, `/editor` ו-`/portal/` |
| "החודש מוקפא" בכל שמירה | זה מכוון. לשונית **חודשים** → ביטול הקפאה |
| הרצת שיבוץ נכשלת עם קוד 9009 | גרסה ישנה של `app.py`. בחדשה זה מתוקן |
| הנתונים נעלמו אחרי דיפלוי | `DATABASE_URL` לא מוגדר ב-Render |

---

## בדיקה שההתקנה תקינה

```powershell
python tests\test_portal_archive.py
python tests\test_editor_contract.py
```

שתיהן צריכות להסתיים ב-`ALL CHECKS PASSED`. הן רצות בלי שרת ובלי אינטרנט.
